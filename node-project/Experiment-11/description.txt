Build an Express.js server with RESTful APIs to manage a playing card collection using an in-memory array. It should support listing all cards, adding a card (with suit and value), retrieving a card by ID, and deleting a card by ID, with proper JSON responses for GET, POST, and DELETE requests
